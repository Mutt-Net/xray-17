# Sprint s3 — Implementation Plan

**Goal:** Land the scheduled s3 tickets — Track A start (RND-01/02/03) and the Track B
render-regression harness scaffold (TEST-01/02) — on branch `sprint/s3`.

**Spec:** `docs/superpowers/specs/2026-06-03-sprint-s3-design.md`
**Harness design:** `docs/superpowers/specs/2026-06-03-render-regression-harness.md`

> Read the spec first. RND-01's backlog framing is corrected there: the cube-RT path is
> already complete for DX10/11 (s2) and intentionally absent for DX9; the s3 action is
> cleanup + a guarded dump line, not "finishing a port".

---

## Task RND-01 — Remove dead `/* DX10 cut */` CRTC blocks (cleanup)

**Files:** `src/Layers/xrRender/ResourceManager_Resources.cpp`, `src/Layers/xrRender/SH_RT.cpp`
(both R2/DX9-only translation units; `CRTC`/`_CreateRTC` are `#if`-guarded out there).

- [ ] Delete the `/* ... */` `_CreateRTC`/`_DeleteRTC` block (`ResourceManager_Resources.cpp` ~372–403).
- [ ] Delete the `/* DX10 cut */` `CRTC` class block (`SH_RT.cpp` ~119–203).
- [ ] Leave a single-line pointer comment to `xrRenderDX10/dx10SH_RT.cpp` /
      `dx10ResourceManager_Resources.cpp` as the DX10/11 home of cube RTs.
- [ ] No behavioural change (dead code only).

## Task RND-02 — Restore `rtargetsc` dump under DX10/11 guard (tech-debt)

**File:** `src/Layers/xrRender/ResourceManager_Reset.cpp` (shared, all backends).

- [ ] Replace the commented `// DX10 cut ... mdump(m_rtargets_c)` line at ~128 with a real
      census, wrapped in `#if defined(USE_DX10) || defined(USE_DX11)`.

## Task RND-03 — Resolve `set_xform` stub (tech-debt)

**File:** `src/Layers/xrRenderDX10/dx10R_Backend_Runtime.h`.

- [ ] Replace `//VERIFY(!"Implement CBackend::set_xform");` with a knowledge comment recording
      the confirmed-dead disposition (no fixed-function transform stack on DX10+; sole caller
      `set_Matrices` is `_EDITOR`-only). Keep `stat.xforms++`.

## Task TEST-01/02 — Render-regression harness — ~~scheduled~~ **MOTHBALLED (de-scoped 2026-06-03)**

**Removed from sprint scope by owner decision.** End-to-end golden-image capture needs the
Anomaly `gamedata` asset set + a render-capable runner the project does not maintain, and the
owner elected not to pursue screenshot-based capture. TEST-01/02 are **not scheduled** — parked,
not pending.

The comparator + fixture template + design doc are retained under `tools/render_regression/`
for future revival; the CI workflow was moved to
`tools/render_regression/render-regression.yml.parked` (de-registered from CI). See the triage
doc's "Sprint s3 dispositions → Epic TEST" entry for the revival recipe.

## Verification

- [ ] `cmake --build --preset MSVS.MSVC.Ninja.RelWithDebInfo` — clean incremental build, all 4 targets.
- [ ] `python tools/render_regression/compare.py --selftest` — comparator self-test passes.
- [ ] `grep -rn "DX10 cut" src/Layers/xrRender/ResourceManager_Resources.cpp src/Layers/xrRender/SH_RT.cpp` — no hits.
- [ ] Update `docs/render-todo-triage.md`: fix the stale "E still open" footer (Group E was
      resolved in s2, commit 527180c6); record RND-01/02/03 disposition.
- [ ] Update `docs/muttcode-changes.md` with the s3 entries.

## Commit policy

Per standing instructions, commits await explicit approval. Make all edits, build-verify, then
present for sign-off; do not push.
